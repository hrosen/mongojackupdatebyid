apply plugin: 'java'
apply plugin: 'idea'

sourceCompatibility = 1.7

repositories {
    mavenCentral()

}

dependencies {
    compile 'org.mongojack:mongojack:+'

    testCompile 'junit:junit:+'
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.1'
}
